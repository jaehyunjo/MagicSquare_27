"""Track B — D-LOC-01 blank coordinate locator RED skeleton (Report/09)."""

from __future__ import annotations

import pytest

# from entity.locator import find_blank_coords


def test_d_loc_01_g1_blank_coords_row_major() -> None:
    """D-LOC-01 | G1 → row-major blanks (2,2), (3,3) 1-index."""
    # Given — G1
    # matrix = ...
    # When — find_blank_coords(matrix)
    # coords = find_blank_coords(matrix)
    pytest.fail("RED: D-LOC-01 — G1 blank coords row-major (2,2) (3,3)")

# Magic Square 4×4 — GREEN 커밋 산출물 (AC-FR-01-01)

| 항목 | 내용 |
|------|------|
| **프로젝트명** | MagicSquare_XX |
| **문서 유형** | GREEN `feat:` 커밋별 필수 산출물 (템플릿 + 기록) |
| **작성 기준일** | 2026-05-29 |
| **브랜치** | `stabilize/green` |
| **범위** | ECB AC-FR-01-01 · G-01 ~ G-04 |
| **상위 문서** | [Report/12](12.MagicSquare_RED_GREEN_Todo_Checklist_Report.md), [Report/11](11.MagicSquare_GREEN_Stabilize_AC_FR_01_01_Report.md) |

---

## 0. 커밋별 필수 산출물 (템플릿)

각 GREEN `feat:` 커밋마다 아래 **6항목**을 기록한다.

```text
1. RED / GREEN 상태 (1줄)
2. 선택 RED (Test ID + node id)
3. 수정한 프로덕션 파일 + 채운 분기 (2~3문장)
4. pytest 결과 (PASS/FAIL)
5. GUI 수동 결과 (해당 시 1~2줄, 아니면 N/A)
6. Conventional Commit 메시지 제안
```

| # | 작성 규칙 |
|---|-----------|
| 1 | 커밋 직전·직후 RED/GREEN 비율 또는 슬라이스 상태를 한 줄로 |
| 2 | 이번 커밋이 green 처리한 Test ID와 pytest **node id** 전체 |
| 3 | `src/` 변경 파일과 **이번에만** 채운 `if` 분기 (리팩터·범위 밖 금지) |
| 4 | 해당 슬라이스 검증 명령 결과 (`passed`/`failed` 건수) |
| 5 | Boundary CLI/UI 없으면 **N/A** |
| 6 | 실제 커밋 메시지 또는 권장 `feat:` 문구 |

---

## G-01 · `6fc9679`

### 1. RED / GREEN 상태 (1줄)

RED 12 failed → **GREEN partial** null 슬라이스 6건 통과, size 6건 RED 유지 (`NotImplementedError`).

### 2. 선택 RED (Test ID + node id)

| Test ID | node id |
|---------|---------|
| U-IN-01 / TC-011 V-01 | `tests/unit/boundary/test_ac_fr_01_01_invalid_size.py::TestAcFr0101InvalidSize::test_none_grid_returns_invalid_size_and_message` |
| U-IN-01 | `...::test_none_grid_exact_message_prd_section_81_wording` |
| U-IN-01 | `...::test_none_grid_returns_validation_failure_model_type` |
| U-IN-01 | `...::test_none_grid_orchestrator_resolve_zero_calls` |
| U-IN-01 | `...::test_none_grid_boundary_resolve_zero_calls` |
| U-IN-01 | `...::test_none_grid_resolve_called_fails_isolation` |

### 3. 수정한 프로덕션 파일 + 채운 분기

- `src/magicsquare/boundary/validation.py` — `grid is None`이면 `ValidationFailure(code="INVALID_SIZE", message="Grid must be 4x4.")` 반환.
- `src/magicsquare/control/orchestrator.py` — `grid is None`이면 `validate_grid_input` 위임 후 조기 반환(`resolve` 미호출).

### 4. pytest 결과

```powershell
pytest tests/unit/boundary/test_ac_fr_01_01_invalid_size.py -k none_grid -v
```

**PASS** — 6 passed, 6 deselected (전체 12 collected 시 6 passed / 6 failed).

### 5. GUI 수동 결과

N/A — ECB Boundary 단위·Control 경로만 검증, UI/CLI 미구현.

### 6. Conventional Commit 메시지

```text
feat: AC-FR-01-01 green slice for grid=None INVALID_SIZE rejection
```

---

## G-02 · `523e795`

### 1. RED / GREEN 상태 (1줄)

GREEN partial 6/12 → **GREEN partial** `[]` 슬라이스 +2건 통과 (8/12).

### 2. 선택 RED (Test ID + node id)

| Test ID | node id |
|---------|---------|
| U-IN-02 / TC-011 V-02 | `tests/unit/boundary/test_ac_fr_01_01_invalid_size.py::TestAcFr0101InvalidSize::test_boundary_shape_returns_invalid_size_failure[empty_list]` |
| U-IN-02 | `...::test_empty_list_returns_invalid_size_code` |

### 3. 수정한 프로덕션 파일 + 채운 분기

- `src/magicsquare/boundary/validation.py` — `isinstance(grid, list) and len(grid) == 0`이면 동일 `INVALID_SIZE` `ValidationFailure` 반환. 그 외 비-list·비-empty는 기존처럼 `NotImplementedError`.

### 4. pytest 결과

```powershell
pytest tests/unit/boundary/test_ac_fr_01_01_invalid_size.py -k "none_grid or empty_list" -v
```

**PASS** — 8 passed, 4 deselected.

### 5. GUI 수동 결과

N/A

### 6. Conventional Commit 메시지

```text
feat: empty grid [] returns INVALID_SIZE
```

---

## G-03 · `44cc78a`

### 1. RED / GREEN 상태 (1줄)

GREEN partial 8/12 → **GREEN partial** shape ≠ 4×4 슬라이스 +3건 통과 (11/12).

### 2. 선택 RED (Test ID + node id)

| Test ID | node id |
|---------|---------|
| U-IN-02 | `tests/unit/boundary/test_ac_fr_01_01_invalid_size.py::TestAcFr0101InvalidSize::test_boundary_shape_returns_invalid_size_failure[four_empty_rows]` |
| U-IN-02~03 | `...::test_boundary_shape_returns_invalid_size_failure[three_by_four]` |
| U-IN-03 | `...::test_three_by_four_returns_invalid_size_code` |

### 3. 수정한 프로덕션 파일 + 채운 분기

- `src/magicsquare/boundary/validation.py` — `list` 입력에서 `len(grid) != 4` 또는 행이 `list`가 아니거나 `len(row) != 4`이면 `INVALID_SIZE` 반환. 4×4 형태로 보이는 list는 아직 `NotImplementedError`(다음 Slice).

### 4. pytest 결과

```powershell
pytest tests/unit/boundary/test_ac_fr_01_01_invalid_size.py -k "none_grid or empty_list or three_by_four or four_empty_rows" -v
```

**PASS** — 11 passed, 1 deselected.

### 5. GUI 수동 결과

N/A

### 6. Conventional Commit 메시지

```text
feat: non-4x4 shape returns INVALID_SIZE
```

---

## G-04 · `67eb1cf`

### 1. RED / GREEN 상태 (1줄)

GREEN partial 11/12 → **AC-FR-01-01 GREEN 완료** 12/12 (`INVALID_SIZE` 차원 거부 전 구간).

### 2. 선택 RED (Test ID + node id)

| Test ID | node id |
|---------|---------|
| U-IN-02~03 (scope) | `tests/unit/boundary/test_ac_fr_01_01_invalid_size.py::TestAcFr0101InvalidSize::test_scope_only_invalid_size_not_other_ac_codes` |

### 3. 수정한 프로덕션 파일 + 채운 분기

- `src/magicsquare/control/orchestrator.py` — `isinstance(grid, list)` 입력도 `validate_grid_input`에 위임해 Control 경로에서 list 차원 거부 시 `resolve` 미진입. Boundary `validation.py`는 G-03에서 이미 scope 입력을 처리하므로 변경 없음.

### 4. pytest 결과

```powershell
pytest tests/unit/boundary/test_ac_fr_01_01_invalid_size.py -v
```

**PASS** — 12 passed, 0 failed.

### 5. GUI 수동 결과

N/A

### 6. Conventional Commit 메시지

```text
feat: scope guard INVALID_SIZE for dimension inputs
```

---

## 요약

| 슬라이스 | 커밋 | 누적 passed | pytest (슬라이스) |
|----------|------|-------------|-------------------|
| G-01 | `6fc9679` | 6/12 | PASS (6) |
| G-02 | `523e795` | 8/12 | PASS (8) |
| G-03 | `44cc78a` | 11/12 | PASS (11) |
| G-04 | `67eb1cf` | **12/12** | PASS (12) |

---

*후속 GREEN 커밋(G-05~)도 동일 6항목 템플릿으로 본 문서에 추가한다.*

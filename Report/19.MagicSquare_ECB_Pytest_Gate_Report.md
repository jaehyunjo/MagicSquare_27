# Magic Square 4×4 — ECB pytest Gate 보고서

| 항목 | 내용 |
|------|------|
| **프로젝트** | MagicSquare_XX |
| **문서 ID** | RPT-MS-019 |
| **작성 목적** | 기본 `pytest` 24 failed 원인 규명 및 ECB 게이트 분리 (`addopts`) |
| **범위** | REFACTOR · CI/개발 워크플로 (production 로직 변경 없음) |
| **브랜치** | `refactor/refactor` @ `83234b6` |
| **작업자** | Cursor Agent |
| **작성일** | 2026-05-29 |
| **선행 문서** | [10](10.MagicSquare_RED_DualTrack_Skeleton_Report.md), [16](16.MagicSquare_REFACTOR_Plan_Report.md) WP-5, [17](17.MagicSquare_REFACTOR_Execution_Report.md), [18](18.MagicSquare_Session_Summary_Report.md) |
| **산출 Transcript** | [Prompting/19.MagicSquare_ECB_Pytest_Gate_Prompt_Transcript.md](../Prompting/19.MagicSquare_ECB_Pytest_Gate_Prompt_Transcript.md) |

---

## 0. Executive Summary

`python -m pytest -q` 실행 시 **24 failed**가 보이는 현상은 **Dual-Track Skeleton RED**(`pytest.fail` only)가 기본 수집에 포함되었기 때문이다.  
구현 버그가 아니며, [Report/10](10.MagicSquare_RED_DualTrack_Skeleton_Report.md) RED DoD와 일치한다.

**조치:** `pytest.ini`에 `addopts = -m "not dual_track_red"` 추가 — **WP-5 마무리**.

| 실행 | 변경 전 | 변경 후 |
|------|---------|---------|
| `pytest -q` | 35 passed, **24 failed**, exit **1** | **35 passed**, 24 deselected, exit **0** |
| `pytest -m dual_track_red -q` | *(동일)* | 24 failed, exit **1** *(의도적 RED)* |

---

## 1. Step 0 — 실측 (2026-05-29)

### Git

```text
branch: refactor/refactor
tip:    83234b6 refactor: exclude dual_track_red skeleton from default pytest (ECB gate)
status: clean (본 Export 시점)
```

### pytest

| 명령 | passed | failed | deselected | exit |
|------|--------|--------|------------|------|
| `python -m pytest -q` | 35 | 0 | 24 | **0** |
| `python -m pytest -m dual_track_red -q` | 0 | 24 | 35 | **1** |
| `pytest tests/unit/boundary/test_ac_fr_01_01_invalid_size.py -q` | 13 | 0 | — | **0** |

---

## 2. 문제 분석

### 2.1 24 failed의 정체

| 항목 | 값 |
|------|-----|
| 파일 | `tests/boundary/test_u_*.py`, `tests/entity/test_d_*.py` |
| 건수 | 24 |
| 실패 유형 | `Failed: RED: U-IN-04 …` / `D-VAL-01 …` |
| 본문 | `pytest.fail("RED: ...")` 한 줄 (assert 없음) |
| TDD phase | Dual-Track **Skeleton RED** — green 미착수 |

### 2.2 ECB vs Dual-Track

| 트랙 | 기본 `pytest` | 이유 |
|------|---------------|------|
| **ECB** (`magicsquare.*`, AC-FR) | ✅ 수집·통과 | 판정 Slice S0 게이트 |
| **Dual-Track** (솔버·E00x) | ❌ 기본 제외 | Skeleton은 별도 `red`→`green` 사이클 |

> ECB REFACTOR 브랜치에서 Skeleton을 GREEN으로 바꾸지 **않음** (Report/12 트랙 분리).

---

## 3. 적용 조치

### 3.1 `pytest.ini`

```ini
addopts = -m "not dual_track_red"
```

- WP-5에서 등록한 `dual_track_red` marker와 연동
- production 코드 변경 없음

### 3.2 README §3

- 기본 `pytest` / `pytest -m dual_track_red` 명령 문서화

### 3.3 커밋

| hash | message |
|------|---------|
| `83234b6` | `refactor: exclude dual_track_red skeleton from default pytest (ECB gate)` |

---

## 4. 개발자 실행 가이드

```powershell
# 일상·CI (ECB + GM + unit)
pytest -q
# → 35 passed, 24 deselected

# AC-FR만
pytest tests/unit/boundary/test_ac_fr_01_01_invalid_size.py -v

# Dual-Track Skeleton RED 확인 (의도적 실패)
pytest -m dual_track_red -v

# 전체 수집 (59건, 24 failed 포함) — 레거시 동작
pytest -q -m ""
# 또는
pytest -o addopts= -q
```

---

## 5. REFACTOR / RED Gate 정합

| Gate | 판정 |
|------|------|
| ECB AC-FR 13 passed | ✅ |
| 기본 `pytest` exit 0 | ✅ *(본 보고서 목적)* |
| Dual-Track Skeleton RED 유지 | ✅ (`pytest -m dual_track_red` → 24 fail) |
| Skeleton assert 제거·skip | ❌ 금지 (TDD rules) |

---

## 6. 미완 · 다음 단계

| 항목 | 단계 |
|------|------|
| Dual-Track Full RED | `red` — `pytest.fail` → assert |
| Dual-Track GREEN | `green` — U-IN-04~08 … |
| CI workflow | `pytest -q` 또는 `pytest -m "not dual_track_red"` 명시 |
| develop merge | `refactor/refactor` PR |

---

## 7. Artifact Index

| 산출물 | 경로 |
|--------|------|
| 본 보고 | `Report/19.MagicSquare_ECB_Pytest_Gate_Report.md` |
| Transcript | `Prompting/19.MagicSquare_ECB_Pytest_Gate_Prompt_Transcript.md` |
| 설정 | `pytest.ini` |
| Skeleton SSOT | [Report/10](10.MagicSquare_RED_DualTrack_Skeleton_Report.md) |

---

*RPT-MS-019 · `refactor/refactor` @ `83234b6`*
